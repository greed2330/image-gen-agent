# 피드백 — 문서 간 원칙 드리프트 (반복 패턴)

> 날짜: 2026-06-10 | 발견: 세션 종료 자체 리뷰 (Opus) | 성격: 자체 발견·수정

## 무엇

08 신규 작성 시 NSFW 담당 모델을 "**abliterated 기본**"으로 적음 → 07 §1-A 원칙 2 "**abliterated는 기본이 아니라 거부 시 폴백**(qwen3:14b 임상 분류 우선)"과 정면 충돌. 같은 세션 자체 리뷰에서 발견·수정.

## 왜 생겼나

새 문서를 쓰며 참조 원칙(07)의 **정확한 표현을 다시 열어보지 않고 기억으로 옮김.**
→ 이건 **반복 패턴**:
- session_002: CLAUDE.md 불일치 3건
- 이번 세션 A1·A2: 01·07에 옛 모델명 `Qwen2.5-VL` 잔재 (session_002 모델 교체 미전파)
- 이번 세션: 07↔08 폴백/기본 충돌

모델 역할·라우팅처럼 **여러 문서에 퍼지는 사실**이 드리프트의 단골.

## 어떻게 방지

1. **인용 시 원문 고정**: 다른 문서의 결정을 적을 땐 그 문서를 열어 문장을 그대로 맞춘다(기억 금지).
2. **단일 출처(canon) 원칙**: 모델 배정 = `03` 배정표, 레이어/폴백 원칙 = `07 §1-A`를 캐논으로. 다른 문서는 *재서술 대신 링크*. 재서술이 불가피하면 캐논과 1:1 대조.
3. **결정 변경 시 전파 체크**: 모델·라우팅·파라미터 변경 시 → 해당 용어를 전 문서 grep 후 일괄 갱신(이번 Qwen2.5-VL처럼 잔재 방지).
