# session_011 — 2026-06-24 — 백호 검증 + 의도해석 버그수정 + 진행률 스트리밍

## 세션 목표
"저번에 문제 삼았던" 백호 정체성 문제가 실제로 해결됐는지 검증하고, 발견되는 문제를 재현 기반으로 진단·수정. 이후 다음 단계(WebSocket 진행률) 진행.

## 주요 결정 (무엇을·왜·버린 대안)

### 백호 정체성 — dry-run 실측 확정
- `cli/trace.py`의 죽은 `_make_orchestrator` 스텁을 실제 `deps` 연결로 복구 → 실제 ollama+TIPO로 dry-run 가능.
- 백호 정체성 보존(Doc 09) 정상 작동 확인: identity 강조 + 모순 색(brown hair/eyes) negative 이동.

### 배경/시간대 오염 가설 — 재현으로 반증
- 처음 본 "gym→simple background", "night→day" 오염을 12개 프롬프트로 재현 시도 → **둘 다 비재현 1회성**.
- **결정: 새 속성군(background/time_of_day) 추가 안 함.** 1회 관측으로 속성군 만들면 과설계(저 Gemini/연구AI가 빠진 함정). 증거 없으면 안 만든다.

### 의도해석 폴백(~52초) — 설정 버그로 확정
- 증상: 매 요청 "JSON parse failed, retrying with abliterated". 첫 브리핑서 "qwen3:14b 멍청" 오진.
- 재현: qwen3:14b 직접 호출은 완벽 파싱. 파이프라인은 `runtime.get_chat_model()` 사용 → `data/runtime_settings.json`이 **VL 8B 모델**로 설정돼 **빈 출력**.
- 수정: runtime 모델 qwen3:14b 복원 + `is_chat_compatible()`로 embed/vl 모델 채팅 후보 제외(GET 필터 + PUT 검증) + 폴백 로그에 실패 모델명 기록.
- 디버깅 기록: `docs/기타/디버깅기록/2026-06-24-intent-빈출력-VL모델.md`.

### 진행률 WebSocket 스트리밍 (Phase 4 마지막 항목)
- **백엔드 릴레이 방식 채택** — 프론트가 ComfyUI(8188)에 직접 붙는 더 짧은 길은 "백엔드가 모델 I/O 소유" 원칙 위반이라 버림.
- 단일 사용자 → ProgressHub broadcast로 단순화(per-request 라우팅 불필요).
- ComfyUI WS `progress {value,max,prompt_id}` 소비 → `/ws/progress`로 중계. 의존성 `websockets`는 uvicorn[standard] 경유 기설치(신규 설치 없음).
- 프론트: 타이머 시뮬레이션 제거 → 실측 %. 준비 단계(LLM 언로드·체크포인트 로드)는 ComfyUI 무이벤트라 "준비 중 …" 0% 유지 후 샘플링부터 실측(가짜 제거 트레이드오프, 의도적).

### 커밋 정책 변경 (오너 신규 상시 지침)
- 앞으로 실무 수준 잦은 커밋(논리 단위 자율 커밋). 푸시는 오너 요청 시. CLAUDE.md + 피드백 문서 반영.

## 산출물 (생성/수정된 파일)
**신규:** `app/services/progress_hub.py`, `app/api/routes_ws.py`, `tests/test_progress_hub.py`, `tests/test_routes_models.py`, 디버깅기록·피드백·본 세션로그
**수정:** `app/api/routes_models.py`, `app/pipeline/intent_parser.py`, `app/pipeline/orchestrator.py`, `app/clients/comfyui_client.py`, `app/deps.py`, `app/main.py`, `cli/trace.py`, `frontend/lib/api.ts`, `frontend/app/page.tsx`, `CLAUDE.md`, 체크리스트
**실질 설정:** `data/runtime_settings.json` (gitignore — VL→qwen3:14b 복원)

## 검증
- 백엔드 테스트 50개 통과 (45→50). 프론트 `tsc --noEmit` 클린.
- API 실측: `GET /models` VL/embed 제외, PUT 부적합 모델 400. DB 영속화 방 복원.
- 풀 e2e: 백호 프롬프트 → 실제 이미지(53초, critique passed, **정체성 시각 확인**). 깨진 옛 테스트 방 삭제 정리.
- WS 릴레이: `/ws/progress` 28스텝 pct 3→100 **단조증가 실측**, 실제 value/max.

## 미해결 / 다음 세션으로 넘기는 것
- **Phase 4 (프론트엔드) 완료.** 남은 건 Phase 3 (설계 선행 필요).
- Phase 3: qwen3-vl 레퍼런스 의미분석 → IPAdapter/ControlNet 분기, inpaint, Critic 루프, mem0.
- 브라우저 픽셀 단위 검증(진행률 바 렌더)은 자동화 핸들 부재로 미실시 — 오너 직접 권장.
- img2img 실검증 미실시.
- danbooru_tags.csv 미확보 (allowlist 비활성).
