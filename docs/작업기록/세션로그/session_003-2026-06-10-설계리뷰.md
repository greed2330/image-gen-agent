# session_003 — 전체 설계 리뷰 + NSFW 문서 명문화

> 날짜: 2026-06-10 | 모델: Opus 4.8 | 이전: session_002 (Sonnet, NSFW설계심화 + 코드 스켈레톤)

---

## 세션 목표

session_002가 "Opus 상의 필요"로 넘긴 2건(NSFW 레이어 분리 명문화 / 08 문서) 처리 → 리소스 분배 전략 점검 → 전체 설계 리뷰(자체 세미나).

---

## 주요 결정 (무엇을·왜·버린 대안)

### 1. NSFW 레이어 분리 원칙 → 07 §1-A 명문화
- session_002에서 "라벨만 있고 원칙 없음"으로 지적된 구멍. 방향은 이미 확정(임상적 핸드오프)이라 **설계 토론이 아닌 받아쓰기**.
- 추가: 원칙 1 임상적 핸드오프(검열 모델을 "분류"로 프레이밍 → 거부율↓) / 원칙 2 에스컬레이션(**abliterated는 기본이 아니라 거부 시 폴백**) / 원칙 3 동일 출력 계약 / 경계선 = Phase 2 실측.

### 2. 08 신규 작성 — 가설 기반 구조 (오너 지시)
- 오너 지시: "중간 분량 + **가설을 먼저 세우고 그에 맞춰** 나가기." TIPO NSFW 품질 미실측이라 단정 대신 H1~H4 가설 + Phase 2 검증법 + 깨질 때 대응으로 구성.
- 위상 축소 근거: TIPO가 어휘·조합·정렬을 가져감 → 거대 사전 불필요. 08은 TIPO가 못 하는 **한국어→씨앗 변환** 보완(중복 아님).
- 분량 = 중간(카테고리당 10~15, 프리셋 6). 금지 시드(미성년 계열) allowlist 하드드롭 명시.

### 3. 02 VRAM 갱신 (리소스 분배 점검 결과)
- 발견: 02가 두뇌를 7–8B로 가정했으나 session_002에서 **14B(qwen3:14b 9.3GB)**로 교체됨 → 공존 계산이 깨져 있었음.
- 수정: VRAM 표 실수치 + TIPO 행 / 공존표 재계산(**14B 두뇌+SDXL=17.3GB 불가, 공존은 qwen3:4b만**) / 다단계 스왑 타임라인 / Critic 재시도 상한.

### 4. 전체 설계 리뷰(자체 세미나) — 신규 문서
- 강점 5 / 정합성버그(A) / 구조리스크(B1~B6) / 미정결정(C1~C5) / Phase 1 준비도.
- **가장 큰 리스크 B3**: hallucination 대책 전체가 TIPO 단일 의존인데 NSFW 미검증 → Plan B 미문서화.
- 오너 결정: **C5 Critic = Phase 3로 격하**(초기엔 생성→사용자 확인). A1·A2 즉시 수정.

### 5. 자체 리뷰에서 발견·수정한 실수
- 07↔08 충돌: 08이 abliterated를 NSFW "기본"으로 적어 07 "폴백" 원칙과 모순 → 모델 비종속으로 수정.
- 전체 grep으로 Qwen2.5-VL 라이브 잔재 추가 2곳(07 §3-3, 체크리스트 Phase 3) 발견·수정.
- **문서 간 원칙 드리프트가 반복 패턴**(CLAUDE.md 3건→A1/A2→07↔08) → 피드백 문서 작성.

---

## 산출물 (생성/수정된 파일)

| 파일 | 작업 |
|------|------|
| `설계문서/07-...전략.md` | §1-A NSFW 분리 원칙 블록 추가 + Qwen2.5-VL 잔재 2곳 교체 |
| `설계문서/08-NSFW-시드-매핑-및-장면-프리셋.md` | **신규** — 가설(H1~H4) + 한국어→씨앗 매핑 + 장면 프리셋 + 금지 시드 |
| `설계문서/02-...VRAM...md` | VRAM 표·공존표·상태머신·Critic 상한 14B 기준 갱신 |
| `설계문서/01-시스템-아키텍처.md` | Qwen2.5-VL → qwen3-vl-abliterated:8b (3곳) + Critic Phase 3 표기 |
| `기타/설계리뷰/2026-06-10-전체설계리뷰-세미나.md` | **신규** — 전체 리뷰 |
| `기타/피드백/2026-06-10-문서간-원칙-드리프트.md` | **신규** — 드리프트 방지 |
| `작업기록/체크리스트.md` | 다음액션 1·2 완료, 설계리뷰 결과·C1~C4 잔여, Phase 3 잔재 수정 |

---

## 미해결 / 다음 세션으로 넘기는 것

1. **[실측 의존 — Phase 1/2]** 08 H1~H4 (TIPO NSFW 확장·매핑 효과·프리셋·표기 일치). B1 스왑 지연, B2 거부 경계.
2. **[구현 시 설계 — Phase 2]** C2 런타임 거부 탐지 방식 / C3 TIPO Plan B(DanTagGen·수동사전 복원) / C4 mem0 추출 모델 배정.
3. **[Phase 1 착수 전]** C1 생성 1회 지연 목표 대략값.
4. **[실행]** `ollama rm qwen3-vl:8b` (기존 검열 비전 모델 제거).
5. **[다음 단계]** Phase 1 환경 셋업: Git init → ComfyUI 설치 → 체크포인트 → VRAM 실측.
