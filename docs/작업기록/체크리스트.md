# 작업 체크리스트 (현황)

> 이 문서는 **현재 작업의 단일 진실 공급원(SSOT)**. 작업 시작 시 읽고, 진행하며 갱신한다.
> 완료된 큰 단위는 `완료보관/`으로 이관. 최종 수정: 2026-06-18 (session_010 — Doc 09~13 전부 완료)

## 범례
- [ ] 미착수  / [~] 진행중  / [x] 완료  / [!] 막힘(블로커)

---

## Phase 0 — 설계 (완료)

- [x] 프로젝트 폴더 생성 (`E:\image-gen-agent`)
- [x] docs 폴더 구조 생성 (설계문서/작업기록/기타)
- [x] 웹 조사: 최신 모델·VRAM·mem0·ollama 무검열
- [x] 설계 문서 작성 (00~05, 07, 09~13)
- [x] CLAUDE.md 작성 + 전체 영어 재작성 + 문서 언어 규칙
- [x] 언어/프레임워크 확정: **Python(FastAPI) + Next.js/TS**
- [x] 무검열 모델 선정 + pull (qwen3-abliterated:14b, qwen3-vl-abliterated:8b)

## Phase 1 — 환경 셋업 (완료)

- [~] GitHub 레포 초기화 + .gitignore — **최초 커밋은 오너 요청 대기**
- [x] ComfyUI 설치/확인 + API 접근 테스트 (`E:\ComfyUI`, port 8188)
- [x] 체크포인트 설치: WAI-illustrious v13.0, NoobAI-XL 1.1
- [x] ControlNet union promax + IPAdapter plus/plus-face (vit-h) + CLIP-ViT-H 설치
- [x] TIPO 설치 (tipo-kgen, TIPO-500M-ft, CUDA, backend/.venv)
- [x] VRAM 실측: SDXL peak 8.6GB, qwen3:14b 11.2GB, 시분할 스왑 정상

## Phase 2 — 백엔드 코어 (완료)

- [x] 스켈레톤 타이핑 (schemas, pipeline stubs, orchestrator trace, presets, tests)
- [x] FastAPI main.py + 라우터 배선 + CORS + /health
- [x] ComfyUI 클라이언트 + _build_workflow (txt2img.json 슬롯 채우기)
- [x] VRAM 매니저 (LLM unload → ComfyUI → /free 순서)
- [x] OllamaClient (generate/chat/embed/unload/list_models), CloudLLMClient
- [x] IntentParser (qwen3:14b → abliterated escalation, /no_think, few-shot)
- [x] PromptCompiler (TIPO 확장 + 검열태그 negative 강제이동 + 허용목록)
- [x] ParamResolver, WorkflowRouter, Critic stub
- [x] deps.py DI 싱글턴 구성
- [x] 이미지 서빙 엔드포인트 `GET /images/{filename}` + `?download=1`
- [x] **실제 생성 검증** — 전체 파이프라인 end-to-end 동작, 총 소요 ~30초
- [x] **45개 테스트 전부 통과** (2026-06-18 기준)

### 주요 확장 (session_009~010 — 2026-06-18)

- [x] **캐릭터 정체성 보존 (Doc 09)** — identity_tags 강조 가중치(1.3), 속성군 충돌 negative 이동, tag_groups.py 공유 모듈
- [x] **대화 기억 영속화 (Doc 10)** — SQLite + SQLModel, Room/Message/Generation DB, 채팅방 CRUD API, 캐릭터 카드 누적(merge_identity)
- [x] **img2img 워크플로우 (Doc 11)** — upload_image(), img2img.json, WorkflowRouter 분기, denoise 0.55
- [x] **대화 LLM 런타임 교체 (Doc 12)** — GET/PUT /models, runtime_settings.json 영속화, NSFW 모델 고정
- [x] **이미지 다운로드 (Doc 13)** — ?download=1 파라미터, 라이트박스 다운로드 버튼

## Phase 4 — 프론트엔드 (진행중)

- [x] Next.js 16.2.9 + TypeScript + App Router 초기화 (`frontend/`)
- [x] 3단 레이아웃: 좌측 채팅방 목록 / 중앙 채팅 / 우측 최근 생성 갤러리
- [x] 양쪽 사이드바 접기/펼치기
- [x] GenCard: 로딩 진행 바 → 3D flip 애니메이션 → 실제 이미지
- [x] 라이트박스 (클릭 확대, ESC 닫기, 다운로드 버튼)
- [x] 4가지 테마 (dark/light/slate/sand) + 설정 화면
- [x] 레퍼런스 이미지 첨부 UI (+버튼 → 파일선택 → 썸네일 프리뷰 → base64 전송)
- [x] 대화 history 백엔드 전달 (맥락 유지)
- [x] 채팅방/history 영속화 — 새로고침·재시작 복원 (SQLite 백엔드 연동)
- [x] 설정 화면 대화 모델 드롭다운 (GET /models 연동)
- [ ] 생성 진행률 WebSocket 스트리밍 (현재 타이머 시뮬레이션)

## Phase 3 — 고급 기능 (미착수, 설계 필요)

- [x] img2img 워크플로우 (Doc 11에서 완료 — 레퍼런스 → 직접 변형)
- [ ] 레퍼런스 이미지 의미 분석 (qwen3-vl) → IPAdapter(캐릭터 이식) / ControlNet(포즈 이식) 분기
- [ ] inpaint 워크플로우
- [ ] 자기평가 Critic 루프 (현재 passthrough stub)
- [ ] 자동 메모리 (mem0/Qdrant) — 방-넘는 전역 취향 기억

---

## 현재 블로커 / 대기

- [ ] git 최초 커밋 (오너 요청 대기)
- [ ] danbooru_tags.csv 확보 (현재 allowlist 비활성 — 없어도 동작은 정상)
- [ ] 프론트엔드 실제 테스트 (브라우저에서 새 기능 직접 확인 권장)
  - DB 영속화: 새로고침 후 방 목록·메시지 복원 확인
  - img2img: 레퍼런스 첨부 후 생성 → 구도 유지 변형 확인
  - 모델 드롭다운: 설정에서 교체 후 의도 해석 모델 변경 확인

---

## 주요 설계 결정 기록

> **캐릭터 프리셋 폐기:** 프리셋도 TIPO에 익사 + 매번 수동 관리 비용 → 오너 거부. 대신 **정체성 보존 + 자동 캐릭터 카드**로 전환(Doc 09·10).

> **백호 색 오진 정정:** "흰색→주황 드리프트"는 헛다리(흰색 정상 렌더). 진짜 문제는 **TIPO가 정체성을 익사시키고 모순 태그(black/multicolored hair)를 주입**하는 것. dry-run trace 실측 확정. → identity_tags 강조 + 속성군 배타 + TIPO 범위 제한으로 해결.

> **img2img 단순화:** "레퍼런스가 있으면 무조건 img2img"로 처리. qwen3-vl로 의도 분석 후 IPAdapter/ControlNet 분기는 Phase 3 별도 설계.

---

## 실행 환경 메모

```powershell
# 백엔드 (반드시 venv Python 사용)
E:\image-gen-agent\backend\.venv\Scripts\python.exe -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8000

# 프론트엔드
cd E:\image-gen-agent\frontend; npm run dev
# → http://localhost:3000

# ComfyUI (별도 터미널에서 실행 중이어야 함, port 8188)

# 테스트
E:\image-gen-agent\backend\.venv\Scripts\python.exe -m pytest backend/tests/ -v
```

## 세션 로그 목록

| 번호 | 날짜 | 주제 | 주요 산출물 |
|------|------|------|------------|
| 001 | 2026-06-10 | 초기설계 | 설계문서 00~05, CLAUDE.md |
| 002~005 | 2026-06-10~17 | 환경셋업/백엔드초기 | Phase 1~2 구축 |
| 006 | 2026-06-18 | 백엔드코어 | 파이프라인 완성, end-to-end 검증 |
| 007 | 2026-06-18 | 프론트엔드및파이프라인수정 | 프론트엔드 구축, 버그 수정 |
| 008 | 2026-06-18 | Opus설계 | 설계문서 09~13 작성 |
| 009 | 2026-06-18 | DB영속화구현 | Doc 09(정체성보존) + Doc 10(SQLite 영속화) |
| 010 | 2026-06-18 | img2img및모델교체 | Doc 11(img2img) + Doc 12(LLM교체) + Doc 13(다운로드) |
