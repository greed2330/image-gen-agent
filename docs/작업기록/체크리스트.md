# 작업 체크리스트 (현황)

> 이 문서는 **현재 작업의 단일 진실 공급원(SSOT)**. 작업 시작 시 읽고, 진행하며 갱신한다.
> 완료된 큰 단위는 `완료보관/`으로 이관. 최종 수정: 2026-06-10

## 범례
- [ ] 미착수  / [~] 진행중  / [x] 완료  / [!] 막힘(블로커)

---

## Phase 0 — 설계 (완료)

- [x] 프로젝트 폴더 생성 (`E:\image-gen-agent`)
- [x] docs 폴더 구조 생성 (설계문서/작업기록/기타)
- [x] 웹 조사: 최신 모델·VRAM·mem0·ollama 무검열
- [x] 설계 문서 작성 (00~05)
- [x] 설계 문서 07 작성 (프롬프트·파라미터 전략 — ②③ 핵심)
- [x] CLAUDE.md 작성 (hana 프로젝트 규칙 + Karpathy/awesome-claude-md 레퍼런스 반영)
- [x] skills 도입 계획 레퍼런스 작성 (06번 문서, 실제 제작은 sonnet으로 별도)
- [x] 언어/프레임워크 확정: **백엔드 Python(FastAPI) + 프론트 Next.js/TS, UI 자체 제작**
- [x] ComfyUI 보유 여부 확인: **미보유 → Phase 1에서 신규 설치** (UI는 우리가 만들고 ComfyUI는 API로 조종하는 엔진)
- [x] ollama 보유 모델 확인 + 역할 배정 (03 문서). 결론: **무검열 모델 1개만 추가 pull 필요** (vl·임베딩은 보유)
- [x] 무검열 모델 선정 + pull 완료
  - `huihui_ai/qwen3-abliterated:14b` — NSFW 프롬프트 합성
  - `huihui_ai/qwen3-vl-abliterated:8b` — 비전 교체 (NSFW Critic 커버)
  - ⚠️ 성능 우려 있음 → Phase 2 이후 실제 태그 생성 테스트 필수 (피드백 문서 참고)
  - [x] `ollama rm qwen3-vl:8b` 실행 (구 검열 비전 모델 제거) — 2026-06-18, abliterated 교체본만 잔존
- [x] CLAUDE.md 리뷰 + 불일치 3개 수정 (00~07 참조 / 블로커 정리 / qwen3-vl 보유 반영)
- [x] CLAUDE.md 의사결정 원칙 섹션 추가 (왜 명시 / 가정 금지 / 용어 먼저)
- [x] CLAUDE.md 세션 로그 규칙 + 작업 단위 정의 추가
- [x] 세션로그 폴더 신설 + session_001 이관
- [x] CLAUDE.md 전체 영어로 재작성 + 문서 언어 규칙 추가

## Phase 1 — 환경 셋업 (예정)

- [~] GitHub 레포 초기화 + .gitignore (모델 파일 제외) — 2026-06-17: `git init` + `.gitignore` 완료, 67파일 스테이징. **최초 커밋은 오너 요청 대기.**
- [x] ComfyUI 설치/확인 + API 접근 테스트 — 2026-06-17: `E:\ComfyUI`에 git clone + 전용 venv(Python 3.11.9), torch 2.6.0+cu124(CUDA 인식 ✅), ComfyUI v0.25.0 기동, API `127.0.0.1:8188` `/system_stats` 정상(VRAM 16GB 인식)
- [x] 메인 체크포인트 설치 (WAI-illustrious or NoobAI) — 2026-06-17 완료(HF 미러, 토큰 불필요): WAI `misri/waiNSFWIllustrious_v130`(6.94GB), NoobAI `Laxhar/noobai-XL-1.1`(7.11GB) → `E:\ComfyUI\models\checkpoints`. ComfyUI 인식 ✅
- [x] **첫 테스트 생성 성공** — 2026-06-17: WAI v13.0, SDXL 1024² 20steps euler → 10.5초, 이미지 정상(`output/smoketest_00001_.png`)
- [x] ControlNet + IPAdapter (SDXL) 설치 — 2026-06-18: ControlNet union promax(`xinsir`, 10+ 제어 1개로 커버) → `models/controlnet/`. IPAdapter plus + plus-face(vit-h) → `models/ipadapter/`, CLIP-ViT-H 인코더 → `models/clip_vision/`. 커스텀노드 `ComfyUI_IPAdapter_plus`(cubiq) clone. ComfyUI 재기동 후 노드 35개 + 모델 3종 전부 인식 ✅. (bigG 인코더는 vit-h 통일 위해 생략, 전처리기 aux노드는 Phase 3 레퍼런스 분석 때)
- [x] **TIPO 설치** — 2026-06-18: `tipo-kgen`(pip) + transformers 백엔드 + torch 2.6.0+cu124, **TIPO-500M-ft**, device="cuda". 백엔드 venv(`backend/.venv`)에 설치. 의존성: sentencepiece(토크나이저), torch CUDA(GPU 추론). (GGUF/llama-cpp 백엔드는 Windows DLL 문제로 버림 / CPU는 long 82초로 사용불가)
- [x] **TIPO NSFW 태그 품질 + 표기 관례 일치 실측** — 2026-06-18: SFW·NSFW 씨앗 모두 일관된 danbooru 태그로 확장, **무검열 확인** ✅. GPU ~1초(short 0.9s/long 1.2s), VRAM +1.25GB. ⚠️ **발견: TIPO가 `censored`/`mosaic censoring` 태그 자동 주입** → prompt_compiler에서 negative로 강제 이동 필요(아래 07 기록). C3(TIPO Plan B) 불필요 — TIPO 정상 작동.
- [~] VRAM 실측: SDXL/Flux/LLM 각 점유량 + 언로드 동작 검증
  - [x] 2026-06-17 SDXL(WAI v13.0): baseline 1649MiB → peak **8653MiB**(생성 중). `/free`(unload_models+free_memory) → **1774MiB** 복귀 ✅
  - [x] 2026-06-17 ollama 풀 스왑 검증 ✅: qwen3:4b 로드 4846→언로드 1503. qwen3:14b 로드 **11234MiB**(두뇌 9.6GB)→언로드 1764→SDXL 생성 peak 11076(8.4s)→`/free` 1715. **무한루프 없음, 시분할 정상 동작.**
  - [ ] Flux/Chroma 점유량 (해당 모델 도입 시)

## Phase 2 — 백엔드 코어 (예정)

- [x] **백엔드 스켈레톤 타이핑** (Sonnet, 2026-06-10)
  - [x] schemas.py 데이터 계약 완성 (Intent/Route/Compiled/Params/Critique/Trace/Result)
  - [x] pipeline/clients/services 스텁 + orchestrator 트레이스 골격
  - [x] **CLI 디버그 하버스** (trace/chat/memcheck + `--dry-run`/`--stage`)
  - [x] presets/model_presets.yaml, style_presets.yaml, workflows/txt2img.json
  - [x] tests/ 10개 전부 통과 (모든 외부 의존 mock)
- [ ] FastAPI 스캐폴딩
- [ ] ComfyUI 클라이언트 + 워크플로우 빌더(txt2img 최소 동작)
- [ ] VRAM 매니저 (ollama unload → comfy run → comfy free 흐름)
- [ ] LLM 라우터 (ollama 로컬 + 클라우드 하이브리드)
- [ ] Intent Parser → Prompt Compiler(LLM 씨앗→TIPO→allowlist) → Param Resolver 최소 파이프라인
- [ ] mem0 통합 (로컬 Qdrant + ollama 임베딩)

## Phase 3 — 고급 기능 (예정)

- [ ] 레퍼런스 이미지 분석 (qwen3-vl-abliterated:8b) → ControlNet/IPAdapter 라우팅
- [ ] img2img / inpaint 워크플로우
- [ ] 자기평가 Critic 루프
- [ ] 성공 레시피 저장/재사용

## Phase 4 — 프론트엔드 (예정)

- [ ] Next.js 채팅 UI 스캐폴딩
- [ ] 채팅방 생성/삭제/전환 + 채팅방별 메모리 연동
- [ ] 이미지 첨부 + 생성 결과 갤러리
- [ ] 생성 진행률 WebSocket 스트리밍

---

## 전체 설계 리뷰 (2026-06-10 Opus) — `docs/기타/설계리뷰/2026-06-10-전체설계리뷰-세미나.md`

- [x] 정합성 버그 A1·A2 수정 (01·07 Qwen2.5-VL → qwen3-vl-abliterated:8b)
- [x] 02 VRAM 14B 반영 (스왑 타임라인·공존표·Critic 상한)
- [x] C5 Critic 위상 → Phase 3 격하 확정
- 구조적 리스크 B1~B6 + 미정 결정 C1~C4 (실측/구현 시 해소):
  - [~] C1 생성 1회 지연 목표 설정 — 부분 데이터(2026-06-18): SDXL 생성 8.4~10.5s + TIPO 1.2s. 전체 파이프라인(의도해석+스왑 포함) 1회 지연은 Phase 2 통합 후 확정
  - [ ] C2 런타임 거부 탐지 방식 (Phase 2 구현 — 08 H2와 함께)
  - [x] C3 TIPO Plan B — **불필요 확정**(2026-06-18 TIPO 실측 성공, DanTagGen 폴백 불요)
  - [ ] C4 mem0 추출 모델 배정 (03 배정표 보강)

## 현재 블로커 / 대기

- [ ] 추가할 무검열 모델 1개 선정 (abliterated Qwen3/dolphin 계열 — 검색 후 오너 승인 후 pull)

## 다음 액션 (Opus handoff — 상세는 `prompt.md` 참고)

1. [x] NSFW 레이어 분리 원칙 → 07 §1-A 명문화 (임상적 핸드오프/에스컬레이션/동일출력/경계선) — 2026-06-10 Opus
2. [x] 08번 문서 신규 생성 — NSFW 시드 매핑 & 장면 프리셋 (가설 기반, H1~H4 Phase 2 검증) — 2026-06-10 Opus
3. [x] **Phase 1 환경 셋업 거의 완료** (2026-06-18, session_005): Git init·ComfyUI·체크포인트2종·ControlNet/IPAdapter·TIPO·VRAM 실측 전부 ✅. 남은 것: 최초 git 커밋(오너 요청 대기), skills 설치(06 — 선택), Flux/Chroma 점유량(해당 모델 도입 시).
4. **(다음) Phase 2 아키텍처 구현** — 스켈레톤에 살 붙이기. ComfyUI 클라이언트(검증된 API/워크플로우 재사용), TIPO 클라이언트(검증된 호출부), VRAM 매니저(검증된 스왑 순서). Sonnet 가능.

> ⚠️ 08의 H1~H4 가설은 Phase 1/2 실측 대상. 분량(현 중간)은 검증 후 양방향 재조정.
> 📌 Phase 2 구현 시 재사용할 검증된 자산: txt2img API 페이로드(smoketest), `/free` 언로드, ollama keep_alive:0 스왑, TIPO 호출부(`kgen.executor.tipo`) — session_005 참고.

> 인수인계 문서: `docs/작업기록/prompt.md`, `docs/작업기록/세션로그/session_002-2026-06-10-NSFW설계심화.md`
